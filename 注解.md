
# Java 注解（Annotation）学习笔记

---

## 1. 注解是什么

> 注解是**元数据**，本身不改变代码逻辑，只携带信息。由编译器、框架或运行时反射**读取后决定如何处理**。

| 理解层次 | 说明 |
|----------|------|
| 标记 | 注解就是一个标记，附带配置信息（如 `@Override`） |
| 处理 | 编译器/框架读取注解，做出反应（如 `@Override` 检查是否真的重写） |
| 反射 | 运行时通过 `getAnnotation()` 拿到注解，动态处理 |

---

## 2. 注解本质

- `@interface` 定义 → 编译后是继承 `java.lang.annotation.Annotation` 的**接口**
- 注解属性 → 本质是**抽象方法**，使用时 `属性名 = 值` 赋值
- `@MyAnno(name = "张三")` → JVM 通过**动态代理**生成代理实现类对象

---

## 3. 元注解（4个）

> 元注解 = 放在注解上的注解，控制注解的行为

| 元注解 | 作用 | 取值 |
|--------|------|------|
| `@Target` | 指定能放哪 | `TYPE`(类) / `METHOD`(方法) / `FIELD`(字段) / `PARAMETER`(参数) |
| `@Retention` | 指定保留到何时 | `SOURCE`(源码就丢) / `CLASS`(编译期，默认) / `RUNTIME`(运行时) |
| `@Documented` | 是否被 javadoc 提取 | — |
| `@Inherited` | 子类是否继承父类注解 | — |

> ⚠️ **重点**：自定义注解要反射读取，必须设 `@Retention(RetentionPolicy.RUNTIME)`，否则反射拿不到！

---

## 4. 自定义注解

```java
@Target(ElementType.METHOD) // 只能用在方法上 @Retention(RetentionPolicy.RUNTIME) // 保留到运行时 public @interface MyAnnotation {

text



String name();            // 必填属性
int age() default 18;     // 可选属性，默认18
String sex() default "男"; // 可选属性，默认"男"
}

text




---

## 5. 注解解析（反射）

```java
public class Demo {

text



@MyAnnotation(name = "张三", age = 20)
public void test() {}
text



public void normal() {}
text



public static void main(String[] args) throws Exception {
    for (Method m : Demo.class.getDeclaredMethods()) {
        if (m.isAnnotationPresent(MyAnnotation.class)) {       // 判断有无注解
            MyAnnotation a = m.getAnnotation(MyAnnotation.class); // 获取注解对象
            System.out.println("name=" + a.name() + ", age=" + a.age());
            // 有注解 → 特殊处理
        } else {
            System.out.println(m.getName() + " 无注解，跳过");
        }
    }
}
}

text




**核心API**：

| 方法 | 用途 |
|------|------|
| `isAnnotationPresent(注解.class)` | 判断是否存在注解 |
| `getAnnotation(注解.class)` | 获取注解对象 |
| `getDeclaredAnnotations()` | 获取所有注解 |

---

## 6. 应用场景
核心模式：标记 → 扫描识别 → 差异化处理（有则特殊处理，无则跳过）

text




| 场景 | 注解 | 原理 |
|------|------|------|
| 单元测试 | `@Test` | JUnit 扫描有 `@Test` 的方法自动执行 |
| 依赖注入 | `@Autowired` | Spring 扫描字段自动注入 Bean |
| 接口映射 | `@GetMapping` | Spring MVC 将 HTTP 请求映射到方法 |
| 编译检查 | `@Override` | 编译器检查是否真的重写 |
| JSON忽略 | `@JsonIgnore` | Jackson 序列化时跳过该字段 |
| 权限控制 | 自定义 `@RequireRole` | 反射读取角色值，判断是否放行 |

---

## 7. 复习速记

1. 注解 = 接口 + 元数据，属性 = 抽象方法
2. 元注解 `@Target` 管放哪，`@Retention` 管留多久
3. ⚠️ 反射必加 `@Retention(RetentionPolicy.RUNTIME)`
4. 解析用 `isAnnotationPresent()` + `getAnnotation()`
5. 应用 = 标记 → 识别 → 差异化处理