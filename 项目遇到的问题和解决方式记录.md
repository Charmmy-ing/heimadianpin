markdown



# 黑马点评 — 登录功能实现（Session → Redis + Token 改造）

## 一、项目背景

将登录方案从传统的 **HttpSession** 迁移到 **Redis + Token**，实现分布式会话管理。

---

## 二、技术架构

### 2.1 双拦截器设计
请求 → RefreshTokenInterceptor (order=0) → 有token就刷新+存ThreadLocal，始终放行 → LoginInterceptor (order=1) → 检查ThreadLocal是否有用户，没有则返回401

text




| 拦截器 | 作用 | 是否放行 |
|--------|------|----------|
| `RefreshTokenInterceptor` | 刷新 token 有效期，将用户信息存入 ThreadLocal | 始终放行 |
| `LoginInterceptor` | 校验 ThreadLocal 中是否有用户，判断登录状态 | 无用户时拦截返回 401 |

### 2.2 为什么用两个拦截器？

- **单一职责**：`RefreshTokenInterceptor` 只管 token 刷新，`LoginInterceptor` 只管权限校验
- **即使未登录也能刷新 token**：如果用户之前登录过，带了 token 访问公开页面，也能自动续期

---

## 三、核心代码

### 3.1 发送验证码 — Redis 存储

```java
// UserServiceImpl.sendCode() stringRedisTemplate.opsForValue().set( LOGIN_CODE_KEY + phone, // key: "login:code:138xxxx" code, // 验证码 LOGIN_CODE_TTL, // 2分钟过期 TimeUnit.MINUTES );

text




### 3.2 登录 — Redis 存储用户信息

```java
// UserServiceImpl.login() // 1. 从 Redis 校验验证码 String cashCode = stringRedisTemplate.opsForValue().get(LOGIN_CODE_KEY + phone);

// 2. 生成 token String token = UUID.randomUUID().toString();

// 3. 用户信息存为 Redis Hash Map<String, String> userMap = new HashMap<>(); userMap.put("id", userDTO.getId().toString()); userMap.put("nickName", userDTO.getNickName()); userMap.put("icon", userDTO.getIcon()); stringRedisTemplate.opsForHash().putAll(LOGIN_USER_KEY + token, userMap);

// 4. 设置过期时间（36000分钟） stringRedisTemplate.expire(LOGIN_USER_KEY + token, LOGIN_USER_TTL, TimeUnit.MINUTES);

// 5. 返回 token 给前端 return Result.ok(token);

text




### 3.3 RefreshTokenInterceptor — 刷新 token

```java
public boolean preHandle(...) { String token = request.getHeader("Authorization"); if (token == null) return true; // 没token也放行

text



Map<Object, Object> userMap = stringRedisTemplate.opsForHash()
    .entries(LOGIN_USER_KEY + token);
if (userMap.isEmpty()) return true;  // token无效也放行
text



// 存入 ThreadLocal
UserDTO userDTO = BeanUtil.fillBeanWithMap(userMap, new UserDTO(), false);
UserHolder.saveUser(userDTO);
text



// 刷新过期时间
stringRedisTemplate.expire(LOGIN_USER_KEY + token, LOGIN_USER_TTL, TimeUnit.MINUTES);
return true;
}

text




### 3.4 LoginInterceptor — 校验登录

```java
public boolean preHandle(...) { if (UserHolder.getUser() == null) { response.setStatus(401); return false; } return true; }

text




### 3.5 MvcConfig — 拦截器注册

```java
@Configuration public class MvcConfig implements WebMvcConfigurer { @Resource private StringRedisTemplate stringRedisTemplate;

text



@Override
public void addInterceptors(InterceptorRegistry registry) {
    // 登录拦截器：拦截所有请求，排除公开路径
    registry.addInterceptor(new LoginInterceptor(stringRedisTemplate))
            .addPathPatterns("/**")
            .excludePathPatterns(
                    "/shop/**",        // 浏览店铺 - 公开
                    "/voucher/**",     // 浏览优惠券 - 公开
                    "/shop-type/**",   // 店铺类型 - 公开
                    "/user/code",      // 发验证码 - 公开
                    "/user/login",     // 登录 - 公开
                    "/blog/hot"        // 热门笔记 - 公开
            ).order(1);
text



    // 刷新token拦截器：拦截所有请求
    registry.addInterceptor(new RefreshTokenInterceptor(stringRedisTemplate))
            .addPathPatterns("/**")
            .order(0);
}
}

text




---

## 四、踩坑记录

### 🐛 坑1：重复登录 / 一直跳转登录页

**现象**：登录成功后还是反复跳回登录页面。

**原因**：`addPathPatterns` 和 `excludePathPatterns` 搞反了。

```java
// ❌ 错误：把公开路径写在 addPathPatterns 里，导致公开路径也需要登录 .addPathPatterns("/shop/**", "/user/code", "/user/login", ...)

// ✅ 正确：addPathPatterns 拦截所有，excludePathPatterns 排除公开路径 .addPathPatterns("/") .excludePathPatterns("/shop/", "/user/code", "/user/login", ...)

text




**关键理解**：
| 方法 | 含义 |
|------|------|
| `addPathPatterns` | 需要拦截的路径（需要登录） |
| `excludePathPatterns` | 排除的路径（不需要登录） |

---

### 🐛 坑2：LoginInterceptor 判断逻辑写反

**现象**：已登录用户反而被拦截返回 401。

**原因**：`null` 判断写反了。

```java
// ❌ 错误：用户存在时拦截 if (UserHolder.getUser() != null) { response.setStatus(401); return false; }

// ✅ 正确：用户不存在时才拦截 if (UserHolder.getUser() == null) { response.setStatus(401); return false; }

text




---

### 🐛 坑3：StringRedisTemplate entries() 返回类型不匹配

**现象**：`Map<String, Object>` 编译报错。

**原因**：`StringRedisTemplate.opsForHash().entries()` 返回的是 `Map<Object, Object>`，不是 `Map<String, Object>`。

```java
// ❌ 错误 Map<String, Object> userMap = stringRedisTemplate.opsForHash().entries(key);

// ✅ 正确 Map<Object, Object> userMap = stringRedisTemplate.opsForHash().entries(key);

text




**本质原因**：`StringRedisTemplate` 继承自 `RedisTemplate<String, String>`，其 `HashOperations` 的泛型是 `<String, Object, Object>`，所以 `entries()` 返回 `Map<Object, Object>`。

---

### 🐛 坑4：BeanUtil.copyProperties 单参数用法错误

**现象**：想把 `UserDTO` 转成 `Map` 存 Redis。

**原因**：`BeanUtil.copyProperties(source, target)` 是 Bean 拷贝，单参数调用不合法。转 Map 应该用 `BeanUtil.beanToMap()`。

```java
// ❌ 错误：copyProperties 需要两个参数 Map<String, Object> userMap = BeanUtil.copyProperties(userDTO);

// ✅ 正确：手动构建 Map<String, String>（因为 StringRedisTemplate 需要 String 值） Map<String, String> userMap = new HashMap<>(); userMap.put("id", userDTO.getId().toString()); userMap.put("nickName", userDTO.getNickName()); userMap.put("icon", userDTO.getIcon());

text




**注意**：`StringRedisTemplate` 要求 value 也是 `String`，`Long` 类型的 `id` 必须手动 `toString()`。

---

### 🐛 坑5：LoginInterceptor 构造函数缺少参数

**现象**：`new LoginInterceptor()` 编译报错。

**原因**：`LoginInterceptor` 改造后需要 `StringRedisTemplate` 作为构造参数，但 `MvcConfig` 中还在用无参构造。

```java
// ❌ 错误 registry.addInterceptor(new LoginInterceptor())

// ✅ 正确：注入 stringRedisTemplate 后传入 @Resource private StringRedisTemplate stringRedisTemplate; ... registry.addInterceptor(new LoginInterceptor(stringRedisTemplate))

text




---

### 🐛 坑6：Redis 未启动导致连接失败

**现象**：启动报 `RedisConnectionFailureException: Unable to connect to 127.0.0.1:6379`

**原因**：本地 Redis 服务未启动。

**解决**：启动 Redis 服务即可。
```bash
redis-server

text




---

### 🐛 坑7：@Configuration 注解缺失

**现象**：拦截器配置不生效，断点不进 `addInterceptors` 方法。

**原因**：`MvcConfig` 类缺少 `@Configuration` 注解，Spring 不会把它当作配置类注册。

```java
// ❌ 错误 public class MvcConfig implements WebMvcConfigurer { ... }

// ✅ 正确 @Configuration public class MvcConfig implements WebMvcConfigurer { ... }

text




---

### 🐛 坑8：路径缺少前导 `/`

**现象**：`voucher/**` 路径匹配不到任何请求。

**原因**：路径少了前导 `/`。

```java
// ❌ 错误 "voucher/**"

// ✅ 正确 "/voucher/**"

text




---

## 五、面试要点总结

| 考点 | 要点 |
|------|------|
| **为什么用 Redis 替代 Session？** | Session 共享问题（分布式）、内存压力、无法持久化 |
| **为什么用 Hash 存用户？** | 相比 JSON String，Hash 可以单独读写某个字段，节省带宽 |
| **为什么用两个拦截器？** | 分开 token 刷新和登录校验，实现单一职责 |
| **Token 存在哪里？** | 前端存 `Authorization` 请求头，后端 Redis 存 `login:token:{token}` |
| **如何防止 token 被盗用？** | 设置过期时间 + 每次请求刷新有效期 |
| **ThreadLocal 的作用？** | 同一请求内共享用户信息，避免层层传参，请求结束必须 remove 防止内存泄漏 |
| **拦截器 order 的作用？** | 值越小越先执行，RefreshToken 必须 order=0 先于 LoginIn