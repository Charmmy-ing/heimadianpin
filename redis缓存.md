# Redis 缓存笔记

## 一、缓存是什么？

**缓存（Cache）** 是数据交换的缓冲区，是存储数据的临时地方，其核心特点是 **读写性能高**。

> 本质：用空间换时间，牺牲数据一致性换取高性能。

### 缓存的层级

| 层级 | 示例 |
|------|------|
| 浏览器缓存 | HTTP 强缓存/协商缓存、Cookie、LocalStorage |
| 应用层缓存 | Redis、Memcached、Caffeine（本地缓存） |
| 数据库缓存 | MySQL 查询缓存、索引缓存（Buffer Pool） |
| CPU 缓存 | L1 / L2 / L3 Cache |
| 磁盘缓存 | 磁盘控制器缓存、操作系统 Page Cache |

---

## 二、为什么用缓存？—— 收益与成本

### 收益

- **降低后端负载**：减少数据库的读写压力，避免高频查询直接打到 DB。
- **提高读写效率**：Redis 基于内存，读写速度远超磁盘数据库（通常是微秒级 vs 毫秒级）。
- **降低响应时间**：用户请求更快得到响应，提升用户体验。

### 成本

| 成本类型 | 说明 |
|----------|------|
| **数据一致性成本** | 缓存与数据库数据可能不一致（数据新旧问题），需要设计同步/失效策略 |
| **代码维护成本** | 需要额外编写缓存逻辑（读写、失效、预热），增加代码复杂度 |
| **运维成本** | 需要部署和维护 Redis 集群，处理缓存穿透、击穿、雪崩等问题 |

---

**核心逻辑**：
1. 请求进来，先查 Redis。
2. Redis 命中 → 直接返回。
3. Redis 未命中 → 查数据库 → 数据库返回结果 → **同时写入 Redis**，再返回给客户端。
4. 下次相同请求到来时，Redis 中已有数据，直接命中返回。

---

## 四、业务流程图：商户信息查询

```mermaid
flowchart TD A[开始] --> B["提交商户 ID"] B --> C["从 Redis 查询商户缓存"] C --> D{"缓存是否命中？"}

text



D -->|命中| E["返回缓存中的商户信息"]
E --> F[结束]
text



D -->|未命中| G["根据 ID 查询数据库"]
G --> H{"商户是否存在？"}
text



H -->|存在| I["将商户信息写入 Redis"]
I --> J["返回商户信息"]
J --> F
text



H -->|不存在| K["返回 404 状态码"]
K --> F
text




### 流程文字描述

| 步骤 | 操作 | 分支 |
|------|------|------|
| 1 | 接收商户 ID | — |
| 2 | 查询 Redis 缓存 | — |
| 3 | 判断缓存命中 | **命中** → 直接返回缓存数据，结束 |
| 4 | 缓存未命中 → 查数据库 | — |
| 5 | 判断商户是否存在 | **存在** → 写入 Redis 并返回 |
| 6 | 商户不存在 | **不存在** → 返回 `404` |

---

## 五、后续可优化的方向（进阶）

以上只是最基础的缓存实现，后续还需要考虑：

1. **缓存穿透**：查询不存在的数据（如 id=-1），每次都打到 DB。解决方案：布隆过滤器 / 缓存空值。
2. **缓存击穿**：热点 key 过期瞬间，大量请求打到 DB。解决方案：互斥锁 / 逻辑过期。
3. **缓存雪崩**：大量 key 同时过期或 Redis 宕机。解决方案：TTL 加随机值 / Redis 集群高可用。
4. **缓存与数据库一致性**：先删缓存还是先更新 DB？延迟双删策略。
