redis缓存
缓存是数据交换的缓冲区，是存储数据的临时地方，一般读写性能高；
降低延迟提高速度；
浏览器缓存，应用层缓存，数据库缓存（缓存索引）；cpu和磁盘也会建立缓存；
缓存作用减低后端负载，提高读写效率降低响应时间；
成本：数据一致性成本，（数据新旧的问题）
代码维护成本；
运维成本；
----------------------------
添加redis缓存
没有缓存->客户端到数据库
有缓存->客户端到redis命中返回客户端-》没命中到数据库->数据库返回给客户端->同时写缓存给到redis，使下次相同request在redis里命中
----------------------------
流程：
开始->提交商户id->从redis里查询商户缓存->判断缓存命中--命中-->返回缓存里商户信息->结束                                 --未命中-->根据id查询数据库->判断商户是否存在--存在-->将信息写入redis并返回
                           --不存在-->返回404状态吗；
